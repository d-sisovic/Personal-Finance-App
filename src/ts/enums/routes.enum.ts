export enum ROUTES {
  ROOT = 'root',
  HOME = 'home',
  LOGIN = 'login',
  SIGNUP = 'signup',
  TRANSACTIONS = 'transactions',
  BUDGETS = 'budgets',
  POTS = 'pots',
  NOT_FOUND = 'not-found',
  RECURRING_BILLS = 'recurring-bills',
}
